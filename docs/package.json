{
  "name": "fennecs.tech",
  "version": "0.1.0",
  "description": "fennecs ECS Documentation",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "vitepress dev .",
    "build": "vitepress build .",
    "deploy": "aws s3 sync s3 s3://fennecs.tech --delete && aws s3 sync .vitepress/dist s3://fennecs.tech && aws cloudfront create-invalidation --distribution-id E12V3ZFP8BEESL --paths /",
    "preview": "vitepress preview ."
  },
  "keywords": [
    "documentation"
  ],
  "author": "Tiger Jove",
  "license": "MIT",
  "devDependencies": {
    "markdown-it": "^14.0.0",
    "vitepress": "^1.0.0-rc.44",
    "vitepress-sidebar": "^1.19.0"
  }
}
