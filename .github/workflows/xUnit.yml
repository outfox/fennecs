# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: xUnit

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v5
    - name: Setup .NET
      uses: actions/setup-dotnet@v5
      with:
        dotnet-version: 10.0.x
    - name: Restore dependencies
      run: dotnet restore fennecs.tests/fennecs.tests.csproj --framework net10.0
    - name: Build
      run: dotnet build --no-restore fennecs.tests/fennecs.tests.csproj --framework net10.0
    - name: Test
      run: dotnet test --no-build --verbosity normal fennecs.tests/fennecs.tests.csproj --framework net10.0
