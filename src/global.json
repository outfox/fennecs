{
  "$comment": "Same pin as the repo root global.json (nearest wins). Previously rollForward=latestMajor + allowPrerelease=true, which silently upgraded builds to the .NET 11 preview SDK.",
  "sdk": {
    "version": "10.0.100",
    "rollForward": "latestFeature",
    "allowPrerelease": false
  }
}
