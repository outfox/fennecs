{
  "$comment": "Same pin as the project root global.json (nearest wins, so this keeps a publish started inside this directory working even if the root pin is edited or removed). The web host needs a .NET 10+ SDK with the wasm-tools workload installed.",
  "sdk": {
    "version": "10.0.100",
    "rollForward": "latestFeature"
  }
}
